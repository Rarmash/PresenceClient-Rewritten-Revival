using System;

namespace PresenceClient_CLI;

internal class Program
{
    private static int Main(string[] args)
    {
        Console.WriteLine("The legacy CLI Discord backend has been removed.");
        Console.WriteLine("Use PresenceClient-GUI with the bundled Python backend instead.");
        return 1;
    }
}
